syntax = "proto3";

import "common.proto";

service Broker {
    
    rpc SendCEventLogin (ConnectionIdentity) returns (Result);
    
    rpc RecvCEventLoginRes (ConnectionIdentity) returns (Result);
    
}
